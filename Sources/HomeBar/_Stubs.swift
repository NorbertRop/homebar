// TEMPORARY stubs — replaced by real views in Tasks 16 (MenuContentView) and 17 (SettingsView).
import SwiftUI

struct MenuContentView: View {
    let model: AppModel
    var body: some View { Text("HomeBar (stub)") }
}

struct SettingsView: View {
    let model: AppModel
    var body: some View { Text("Settings (stub)") }
}
